package chess.modules.gameObjects.pieceMove;

import chess.modules.gameObjects.gamePieces.Piece;

public class PawnInitialMove extends PieceMove {
    public PawnInitialMove(int columnPos, int rowPos, Piece piece) {
        super(columnPos, rowPos, piece);
    }
}
